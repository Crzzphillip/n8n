// No-op replacement for Vue directives in a React/Next.js environment.
// If touch events are needed, use a React library (e.g., react-use-gesture) at component level.

export function registerGlobalDirectives(): void {
	// Intentionally empty in Next.js
}
